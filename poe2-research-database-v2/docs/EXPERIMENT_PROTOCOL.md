# Experiment protocol

Each mechanical test records a falsifiable hypothesis, components, controls, procedure, expected result, observations, game patch and evidence.

Use `failed` rather than deleting unsuccessful tests. This prevents repeated dead ends. Do not promote a result to a confirmed interaction until the evidence is reproducible.
